name = "reiatsu-media-proxy"
main = "src/index.js"
compatibility_date = "2024-05-22"

# Usage limits can be managed in your Cloudflare dashboard
# Routes can also be defined here if you have a custom domain:
# routes = [
#   { pattern = "media.yourdomain.com", custom_domain = true }
# ]
